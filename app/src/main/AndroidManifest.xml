<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.bing.lan.view">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/alipay_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".aliPay.AliPayActivity">
        </activity>
        <activity android:name=".youzan.YouZanActivity">
        </activity>
        <activity android:name=".label.LabelButtonActivity">
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".loading.LoadingActivity">
        </activity>
        <activity android:name=".screenLock.ScreenLockActivity">

        </activity>
    </application>

</manifest>